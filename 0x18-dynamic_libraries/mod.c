#include <stdlib.h>
#include <stdio.h>
/**
 * mod  - mod of two integers
 * @a: pointer to the first integer
 * @b: pointer to second integer
 * Return: final answer
 */
int mod(int a, int b)
{
	return (a % b);
}
