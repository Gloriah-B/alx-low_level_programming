#include <stdio.h>
#include <stdlib.h>
/**
 * add  - add two integers
 * @a: pointer to the first integer
 * @b: pointer to second integer
 * Return: addition
 */
int add(int a, int b)
{
	return (a + b);
}
