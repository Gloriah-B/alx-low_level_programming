#include <stdlib.h>
#include <stdio.h>
/**
 * add  - mul two integers
 * @a: pointer to the first integer
 * @b: pointer to second integer
 * Return: multiply
 */
int mul(int a, int b)
{
	return (a * b);
}
